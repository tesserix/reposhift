{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "reposhift-platform.fullname" . }}
  labels:
    {{- include "reposhift-platform.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ .Values.istio.host | quote }}
  gateways:
    - {{ .Values.istio.gateway }}
  http:
    - match:
        - uri:
            prefix: /platform/v1/ws/
          headers:
            upgrade:
              exact: websocket
      route:
        - destination:
            host: {{ include "reposhift-platform.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: {{ .Values.port }}
      timeout: 3600s
    - match:
        - uri:
            prefix: /platform/v1/
      route:
        - destination:
            host: {{ include "reposhift-platform.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: {{ .Values.port }}
      timeout: 30s
{{- end }}
