apiVersion: v1
kind: Service
metadata:
  name: {{ include "migration-ui-dashboard.fullname" . }}
  labels:
    {{- include "migration-ui-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "migration-ui-dashboard.selectorLabels" . | nindent 4 }}
