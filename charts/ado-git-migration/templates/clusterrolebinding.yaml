apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "ado-git-migration.fullname" . }}-manager-rolebinding
  labels:
    {{- include "ado-git-migration.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "ado-git-migration.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "ado-git-migration.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}